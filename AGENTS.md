you need zig 0.14.1

you also need to fetch the git submodules, use --depth=1

https://ziglang.org/download/0.14.1/zig-x86_64-linux-0.14.1.tar.xz

just put the zig binary in your PATH

run zig build and zig build test
