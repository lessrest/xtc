scripts/setup.sh will get you zig and bun

Useful build commands:
- `zig build --summary new` - builds with persistent output summary (progress tree doesn't disappear)
- `zig build test --summary new` - runs tests with persistent output summary (progress tree doesn't disappear)
